{
    "GENEMARK_PATH": "${MAMBA_EXE} run --name braker3 ${HOME}/FunGAP/external/gmes_linux_64_4/gmes_petap.pl",
    "GMHMME3_PATH": "${MAMBA_EXE} run --name braker3 ${HOME}/FunGAP/external/gmes_linux_64_4/gmhmme3",
    "PROBUILD_PATH": "${MAMBA_EXE} run --name braker3 ${HOME}/FunGAP/external/gmes_linux_64_4/probuild",
    "BUILDDATABASE_PATH": "${MAMBA_EXE} run --name repeatmodeler BuildDatabase",
    "REPEATMODELER_PATH": "${MAMBA_EXE} run --name repeatmodeler RepeatModeler",
    "HISAT2_PATH": "${MAMBA_EXE} run --name hisat2 hisat2",
    "TRINITY_PATH": "${MAMBA_EXE} run --name trinity Trinity",
    "MAKER_PATH": "${MAMBA_EXE} run --name maker maker",
    "GFF3_MERGE_PATH": "${MAMBA_EXE} run --name maker gff3_merge",
    "FASTA_MERGE_PATH": "${MAMBA_EXE} run --name maker fasta_merge",
    "MAKER2ZFF_PATH": "${MAMBA_EXE} run --name maker maker2zff",
    "FATHOM_PATH": "${MAMBA_EXE} run --name maker fathom",
    "FORGE_PATH": "${MAMBA_EXE} run --name maker forge",
    "HMM_ASSEMBLER_PATH": "${MAMBA_EXE} run --name maker hmm-assembler.pl",
    "BRAKER_PATH": "${MAMBA_EXE} run --name braker3 braker.pl",
    "BUSCO_PATH": "${MAMBA_EXE} run --name busco busco",
    "PFAM_SCAN_PATH": "${MAMBA_EXE} run --name pfam_scan pfam_scan.pl",
    "BLASTP_PATH": "${MAMBA_EXE} run --name maker blastp",
    "BLASTN_PATH": "${MAMBA_EXE} run --name maker blastn",
    "BLASTX_PATH": "${MAMBA_EXE} run --name maker blastx",
    "MAKEBLASTDB_PATH": "${MAMBA_EXE} run --name maker makeblastdb",
    "SAMTOOLS_PATH": "${MAMBA_EXE} run --name maker samtools",
    "BAMTOOLS_PATH": "${MAMBA_EXE} run --name maker bamtools",
    "AUGUSTUS_PATH": "${MAMBA_EXE} run --name maker augustus",
    "PFAM_DB_PATH": "${HOME}/FunGAP/db/pfam"
}
